# Harness

The Harness turns project intent into repository-local documentation,
executable checks, and CI feedback that coding agents can inspect and improve.

The generated scaffold intentionally begins in a fail-closed state. Follow
`BOOTSTRAP_PROMPT.md` with an AI coding agent to inspect the actual technology
stack, replace placeholders, implement enforcement, and prove the unified
verification command passes.
