# 项目记忆

本文件用于在不同会话和 Agent 之间外置持久上下文，由主协调 AI 根据已验证证据自动维护。

## 维护约定

- 保存稳定的用户偏好、已验证的项目事实、已经建立的 lint/test 命令以及活动执行计划链接。
- 不重复保存任务步骤或检查点；链接的执行计划是任务状态的唯一事实源。
- 不得保存密钥、凭据、无关个人信息、完整对话或未经验证的假设。
- 更新或删除过期事实；必要时注明仓库证据或用户决定。
- Validator 和工作 Agent 将发现返回给主 Agent，不直接修改本文件。

## 用户偏好

- 项目说明文档默认使用中文；路径、命令、状态枚举和协议标识符等技术文本保持原样。（用户于 2026-08-12 明确确认）

## 已验证的项目事实

- 本项目是无可执行代码、无初始化命令的静态 Loop Engineering 脚手架。

## 已建立的验证命令

当前没有 lint 或 test 命令；静态模板不包含可执行代码。

## 活动计划链接

当前没有活动计划。
