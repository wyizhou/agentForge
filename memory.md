# 项目记忆

本文件用于在不同会话和 Agent 之间外置持久上下文，由主协调 AI 根据已验证证据自动维护。

## 维护约定

- 保存稳定的用户偏好、已验证的项目事实、已经建立的 lint/test 命令以及活动执行计划链接。
- 不重复保存任务步骤或检查点；链接的执行计划是任务状态的唯一事实源。
- 不得保存密钥、凭据、无关个人信息、完整对话或未经验证的假设。
- 更新或删除过期事实；必要时注明仓库证据或用户决定。
- Validator 和工作 Agent 将发现返回给主 Agent，不直接修改本文件。

## 用户偏好

- 项目说明文档默认使用中文；路径、命令、状态枚举和协议标识符等技术文本保持原样。（用户于 2026-08-12 明确确认）

## 已验证的项目事实

- 本项目是无可执行代码、无初始化命令的静态 Loop Engineering 脚手架。
- `references/` 是仅由用户主动要求生成或维护的项目知识库，不执行自动沉淀。
- `PLANS.md` 是长期 Roadmap；详细 exec plan 只在任务进入 ready batch 时创建。
- subagent 使用跨平台的 `low`、`medium`、`high` 模型与推理抽象档位，不记录固定模型名称。
- 正式语义变更在 exec plan 中保存冻结验证合同；单次只读/纯排版可以轻量处理，跨会话只读保存简短检查点。Validator 可以使用新检查方法，但阻塞必须绑定已有标准，超范围发现不得改变判定。
- 实施反复失败时按任务、合同版本和失败特征累计尝试；达到“两种修法仍失败、三轮无收敛或直接冲突”任一条件后，由独立只读 Failure Analyst 归因，实现缺陷才可在原边界内修复，规划或合同冲突必须交由人工决定。
- `FAIL` 表示当前输出有明确且可复核的标准违反；`INCONCLUSIVE` 表示当前无法可靠判断，不能直接触发实现修改。Validator 只裁决当前输出，Failure Analyst 才读取失败历史并判断反复失败根因。
- `AGENTS.md` 是核心入口，详细执行协议集中于 `docs/exec-plans/README.md`；串行与实际并行按适用层级完成验证，阻塞按有证据的恢复条件解除。
- 验收标准必须有批准来源；验证绑定合同和受审文件快照，语义变化需重新验证，真实协调回写不递归重验。角色启动按需读取，Validator 不加载 memory 或协调历史。

## 已建立的验证命令

当前没有 lint 或 test 命令；静态模板不包含可执行代码。

## 活动计划链接

- [ADHOC-0011：治理精简与一致性修复](docs/exec-plans/active/ADHOC-0011-governance-simplification.md)
