# Security

- Downloads use HTTPS.
- Skill contents are fetched from an exact resolved commit.
- No downloaded Skill instructions are executed during installation.
- Existing project files are not overwritten.
- Git commit, push, and remote creation are never performed by generated
  scaffolds without an explicit user action.
