# Reliability

- Network failures produce actionable errors and non-zero exit codes.
- Partial target generation is prevented by local preflight.
- A missing optional tool never appears as a successful operation.
- Scripts tolerate target paths containing spaces.
