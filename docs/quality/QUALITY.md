# Quality

Release validation covers POSIX execution, static PowerShell contracts,
generated tree completeness, conflict safety, Git behavior, and Codex review of
an example generated scaffold. It also proves that an empty progressive Harness
passes while newly introduced project code without registered checks fails.
