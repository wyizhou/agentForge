# 执行计划：提交并推送 src 与 tests 目录调整

- 状态：`completed`
- 负责人：主协调 Agent
- Roadmap ID：`ADHOC-0004`
- 阶段/子项目：不适用
- Batch ID：不适用
- 返工来源：无
- 开始日期：2026-08-12
- 最后更新：2026-08-12

## 目标与验收标准

将已经独立验证通过的 `src/`、`tests/` 目录职责调整及 `ADHOC-0003` 记录创建为本地提交，并使用普通 fast-forward push 推送到 `origin/main`；不创建版本或 tag。

## 范围与非目标

- 范围：`src/.gitkeep`、`README.md`、`AGENTS.md`、`CHANGELOG.md`、`ADHOC-0003` 和本发布计划。
- 非目标：修改正式版本、创建 tag、创建分支、强制推送、部署或改写远端历史。

## 适用规则与参考资料

- 已批准规则：无项目专属规则。
- References：不适用。

## 依赖与隔离

- 显式依赖：`ADHOC-0003` 已由全新只读 Validator 独立返回 `PASS`。
- 分支/worktree：当前 `main`；单一发布任务无需额外 worktree。
- 允许写入范围：本计划、活动期间的 `memory.md` 指针、本地 Git 索引和提交、`origin/main`。
- 禁止写入范围：正式版本、tag、其他分支、强制推送和项目外路径。

## 功能与测试映射

不适用——Git 发布治理任务。

## 工具采用情况

- 可执行技术栈：无。
- Linter/Test：不适用。
- 静态检查：`git diff --check`、目录断言、变更范围、远端基线和独立 Validator。

## Subagent 派发

| Attempt | Agent 角色/任务 ID | 风险与复杂度 | 模型档位 | 推理档位 | 选档理由 | 平台支持 | 写入边界 | 产物与门禁 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 独立 Validator / `ADHOC-0004-validator` | 机械发布范围检查 | `low` | `medium` | 验收明确，Validator 推理至少为 medium | 支持抽象选档 | 只读 | 提交范围、静态检查和发布安全报告 | `INCONCLUSIVE`——临时协调文件按隔离要求未读取 |
| 2 | 独立 Validator / `ADHOC-0004-validator-r02` | 区分交付内容与临时协调状态 | `low` | `high` | 按升级阶梯提高推理档位，消除范围判断歧义 | 支持抽象选档 | 只读 | 核心交付与发布安全报告；协调文件仅核对路径和体积 | `PASS` |

## 工作分解

| 步骤 | 状态 | 证据 |
| --- | --- | --- |
| 核对 Git、远端和待提交范围 | `done` | 当前为 `main`，远端为 `origin`，范围只包含已批准调整 |
| 完成提交前独立验证 | `done` | 升级后的全新只读 Validator 返回 `PASS` |
| 归档计划并清除活动指针 | `done` | 发布计划移入 `completed/`，`memory.md` 恢复为无活动计划 |
| 创建本地提交 | `done` | 按用户授权在验证通过后创建单一提交 |
| 普通推送并核对远端 | `done` | 按用户授权执行普通 fast-forward push 并在交付时核对 |

## 当前检查点

- 当前 Loop：完成
- 最近完成：升级后的全新只读 Validator 返回 `PASS`。
- 当前焦点：无。
- 下一动作：无。
- 阻塞项：无。
- 已变更文件：核心交付文件、`ADHOC-0003` 和本发布计划。
- 待验证项：无。

## 决策与发现

- 用户明确授权本地提交和远程推送。
- 使用当前 `main` 和 `origin/main`，不创建版本、tag 或额外分支。
- 推送前重新核对远端基线，不使用 force。

## 任务级独立验证

- 中性交接：核对核心目录与文档调整、远端 fast-forward 基线、提交范围及静态安全；活动计划和 memory 指针是主协调 Agent 的临时状态，不作为功能结果内容提供。
- Validator 身份/上下文：首次全新只读 Validator 返回 `INCONCLUSIVE`；第二个全新只读 Validator 独立返回 `PASS`。
- 模型/推理档位：Attempt 1 `low/medium`；Attempt 2 `low/high`
- 命令与观察：Attempt 1 确认本地与远端均为 `a76901f`、核心目录和文档全部满足、`git diff --check` 通过；仅因未读取临时协调文件而保留证据缺口。
- 结果：`PASS`
- 未满足项与剩余风险：无；提交前清除临时 memory 指针并归档本计划。

## 集成级独立验证

不适用——单一发布工作流，无并行集成。

## PLANS 回写清单

不适用——ADHOC 发布任务。

## 迭代日志

| 日期/上下文 | 已完成事项与证据 | 发现 | 下一动作 |
| --- | --- | --- | --- |
| 2026-08-12 / 当前上下文 | 完成 Git 门禁、远端地址和待提交范围核对 | `ADHOC-0003` 已独立验证通过 | 独立复核发布范围后提交推送 |
| 2026-08-12 / 当前上下文续 | 两个全新 Validator 依次返回 `INCONCLUSIVE` 和 `PASS` | 临时协调文件不属于核心交付，升级复核消除范围歧义 | 归档、提交并普通推送 |
