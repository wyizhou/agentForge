# 执行计划：冻结验证合同与验收漂移防护

- 状态：`completed`
- 负责人：主协调 Agent
- Roadmap ID：`ADHOC-0006`
- 阶段/子项目：`不适用`
- Batch ID：`不适用`
- 返工来源：`无`
- 开始日期：2026-08-21
- 最后更新：2026-08-21

## 目标与验收标准

在静态脚手架中建立“全局 Validator 纪律 + 每任务冻结验证合同”，使 Validator 可以用新方法检查既有标准，但不能通过新增或重解释要求扩大阻塞范围。

## 范围与非目标

- 允许修改：`AGENTS.md`、`README.md`、`CHANGELOG.md`、`docs/exec-plans/README.md`、`docs/exec-plans/template.md`、本计划和活动期间的 `memory.md`。
- 禁止修改：`rules.md`、`PLANS.md`、`skills/`、`references/`、`src/`、`tests/` 和既有 completed 计划。
- 非目标：新增运行时代码、合同目录、linter、测试框架、版本、提交、推送或发布。

## 适用规则与参考资料

- 已批准规则：无。
- 按需读取的 references：无。

## 冻结验证合同

- 合同版本：`VC-001`
- 合同状态：`frozen`
- 冻结依据：用户于 2026-08-21 明确批准本实施计划。
- 冻结时点：Worker 开始仓库修改前。

### 验收标准

| ID | 必须满足的结果 |
| --- | --- |
| `AC-001` | `AGENTS.md` 明确 Validator 不得增加、修改或重解释验收要求，但可用新方法验证已冻结标准。 |
| `AC-002` | exec-plan 说明和模板包含 `VC-*`、`AC-*`、`INV-*`、`TM-*`、`EX-*`、`GATE-*` 及修订记录。 |
| `AC-003` | `FAIL` 必须绑定允许阻塞的标准 ID；超范围发现只能进入 `ADVISORY` 或 `SCOPE_CHANGE_CANDIDATE`。 |
| `AC-004` | 歧义、证据不足或无编号阻塞报告保持 `INCONCLUSIVE/validating`，不得触发代码修改或需求升级。 |
| `AC-005` | 重验证沿用同一合同版本；合同争议不触发模型升级；人工批准升级合同后旧验证失效。 |
| `AC-006` | README 说明双层结构，CHANGELOG 只更新 `Unreleased`，memory 在验证后保存稳定事实。 |
| `AC-007` | 不新增独立合同目录，不修改禁止范围，不提交、不推送、不发布。 |

### 行为不变量

| ID | 必须始终成立的行为 |
| --- | --- |
| `INV-001` | Validator 可以设计合同未列出的检查方法，但只能验证冻结合同和已批准规则。 |
| `INV-002` | exec plan 继续作为任务目标、合同、状态和证据的唯一事实源。 |
| `INV-003` | 主协调 Agent 不得把无效 Validator 报告自行改判为 `PASS`。 |

### 威胁模型

| ID | 范围内风险 |
| --- | --- |
| `TM-001` | Validator 在多轮验证中逐步增加攻击假设、边界或质量要求，形成移动验收标准。 |
| `TM-002` | 更高模型/推理档位把合同争议误判为能力不足并持续扩大验证范围。 |

### 明确排除项

| ID | 本任务不处理的内容 |
| --- | --- |
| `EX-001` | 使用可执行程序强制解析或校验合同结构。 |
| `EX-002` | 修改具体业务项目的现有验收标准、测试或威胁模型。 |
| `EX-003` | 版本发布、Git 提交、远程推送或部署。 |

### Lint/Test 与静态门禁

| ID | 检查 | 预期结果 |
| --- | --- | --- |
| `GATE-001` | 内容检索和场景核对 | 合同结构、裁决规则和重验证规则完整且跨文档一致。 |
| `GATE-002` | Markdown 本地链接检查 | 所有相对链接目标存在。 |
| `GATE-003` | `git diff --check` | 无空白或补丁格式错误。 |
| `GATE-004` | 禁止范围差异检查 | 禁止修改的路径无差异。 |
| `GATE-005` | 全新只读 `high/high` Validator | 返回 `PASS` 且没有未满足项。 |

### 合同修订记录

| 版本 | 状态 | 变更 | 人工批准 |
| --- | --- | --- | --- |
| `VC-001` | `frozen` | 初始冻结合同 | 用户于 2026-08-21 批准 |

## 依赖与隔离

- 显式依赖：无。
- 共享接口和冻结依据：上述 `VC-001`。
- 任务分支：`不适用——单项 ADHOC 治理任务`
- Worktree：`不适用——无并行 Worker`
- 集成分支：`不适用——无并行集成`
- 允许写入范围：见“范围与非目标”。
- 禁止写入范围：见“范围与非目标”。

## 功能与测试映射

`不适用——静态治理文档变更。`

## 工具采用情况

- 可执行技术栈：无。
- Linter 配置和命令：无。
- 测试框架、定向命令和完整命令：无；使用 `GATE-001` 至 `GATE-005`。

## Subagent 派发

| Attempt | Agent 角色/任务 ID | 合同版本 | 风险与复杂度 | 模型档位 | 推理档位 | 选档理由 | 平台支持 | 写入边界 | 产物与门禁 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 独立 Validator / `ADHOC-0006` | `VC-001` | 验证系统治理，存在自指和范围漂移风险 | `high` | `high` | 用户指定且属于高风险验证协议变更 | 支持抽象选档 | 只读，禁止修改仓库 | 按 `VC-001` 返回固定结构化报告 | `PASS` |

## 工作分解

| 步骤 | 状态（`pending`、`in_progress`、`blocked`、`done`） | 证据 |
| --- | --- | --- |
| 完成 Git 门禁并冻结 `VC-001` | `done` | 工作区干净，用户批准内容已写入本计划 |
| 更新全局 Validator 纪律 | `done` | `AGENTS.md` 已加入冻结、绑定裁决、范围候选、歧义和重验证规则 |
| 更新 exec-plan 说明和模板 | `done` | 已加入合同结构、修订历史、固定输出和合同版本记录 |
| 更新 README、CHANGELOG 和项目记忆 | `done` | README 和 Unreleased 已更新；memory 已写入验证后的稳定事实 |
| 运行静态门禁 | `done` | `GATE-001` 至 `GATE-004` 通过 |
| 完成独立 `high/high` 验证 | `done` | 全新只读 Validator 按 `VC-001` 返回 `PASS`，无未满足项 |
| 归档计划并清除活动指针 | `done` | 本计划移入 `completed/`，memory 活动指针已删除 |

## 当前检查点

- 当前 Loop：完成
- 最近完成：全新只读 `high/high` Validator 返回 `PASS`。
- 当前焦点：无。
- 下一动作：无。
- 阻塞项：无。
- 已变更文件：`AGENTS.md`、`README.md`、`CHANGELOG.md`、exec-plan 说明和模板、本计划、`memory.md`。
- 待验证项：无。

## 决策与发现

- 全局纪律写入 `AGENTS.md`，任务专属合同保存在对应 exec plan，不新增独立合同文件或目录。
- 用户对任务或计划的批准同时冻结初始合同；冻结后的任何修改都必须人工明确批准并升级版本。
- 静态检查确认禁止范围无差异、未新增合同目录、Markdown 三个相对链接目标均存在，`git diff --check` 通过。
- 独立 Validator 确认八个语义场景均有明确裁决，`GATE-001` 至 `GATE-005` 全部通过。

## 任务级独立验证

- 逐字冻结合同版本：`VC-001`
- 中性交接：仅提供上述 `VC-001`、适用规则和当前仓库结果。
- Validator 身份/上下文：全新、只读、未参与实施的独立 Agent；未读取 memory、活动计划或历史推理。
- 模型/推理档位：`high/high`
- `criterion_results`：`AC-001` 至 `AC-007`、`INV-001` 至 `INV-003`、`TM-001` 至 `TM-002`、`EX-001` 至 `EX-003`、`GATE-001` 至 `GATE-005` 全部 `PASS`。
- `blocking_findings`：`[]`
- `advisories`：无可执行代码，故无适用 lint/test；稳定事实由主协调 Agent在验证后回写。
- `scope_change_candidates`：`[]`
- `unknowns`：`[]`
- `commands_and_evidence`：完整读取适用文档；核对 Git 范围、冻结合同结构、八个裁决场景、Markdown 链接；`git diff --check` 和禁止路径差异检查通过。
- `overall_verdict`：`PASS`
- 未满足项与剩余风险：无。

## 集成级独立验证

- 结果：`不适用——无并行集成`

## PLANS 回写清单

- [x] Exec plan 已归档到 `completed/`
- [x] Roadmap 叶子任务已更新为 `[x] completed`（不适用——ADHOC）
- [x] 子项目和阶段状态已重新计算（不适用——ADHOC）
- [x] `memory.md` 中的活动计划指针已删除

## 迭代日志

| 日期/上下文 | 已完成事项与证据 | 发现 | 下一动作 |
| --- | --- | --- | --- |
| 2026-08-21 / 当前上下文 | 冻结 `VC-001`，完成治理文档、静态门禁和独立 `high/high` 验证 | 所有合同标准通过，无未满足项 | 任务完成并归档 |
