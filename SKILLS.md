# Project skills

This repository does not vendor runtime skills for its own development.
Generated projects may optionally receive `orchestrate-parallel-work` in both
`.agents/skills/` and `.claude/skills/`, with source metadata recorded by
agentForge.
