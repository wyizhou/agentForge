# 变更日志

本文件记录模板各正式版本的重要变化。普通工作先归入 `Unreleased`；只有用户明确批准发布及版本号后，才能建立正式版本。

## Unreleased

- 精简 AGENTS 核心入口，将详细执行协议集中到 exec-plans 说明，模板改为必填核心与按需章节。
- 按任务影响区分轻量与正式流程，保留语义变更独立验证；修复串行完成、并行集成和阻塞恢复条件。
- 明确角色输入隔离和验收来源，防止未确认假设自动冻结；验证绑定合同与受审快照，真实协调回写免递归重验。
- 以最低充分、可直接升级且不降低已选维度的选档规则替代固定升级路线，保留失败诊断和停止边界。

## 0.4.4 - 2026-08-24

- 新增实施失败诊断门，使用“两种修法仍失败、三轮无收敛或直接冲突”的条件停止普通修复循环。
- 增加独立只读 Failure Analyst、五类失败归因、诊断后修复边界，以及规划冲突必须由人工批准后升级合同的规则。
- 增加 Mermaid 完整流程图和决策表，明确 `FAIL` 是有证据的不满足、`INCONCLUSIVE` 是无法可靠判断，并区分 Validator 与 Failure Analyst 的输入和职责。

## 0.4.3 - 2026-08-21

- 新增“全局 Validator 纪律 + 每任务冻结验证合同”，阻止 Validator 在多轮验证中增加或重解释验收要求。
- 增加带编号的验收标准、不变量、威胁模型、排除项和 lint/test 门禁，以及绑定式 `FAIL`、非阻塞范围候选、合同修订和固定 Validator 输出协议。

## 0.4.2 - 2026-08-12

- 新增初始为空的 `src/` 开发实现目录，明确功能实现位于 `src/`，对应测试位于根目录 `tests/<feature-slug>/`。
- 明确静态模板不使用 `product/` 存放代码或测试，也不预设 `dist/`；未来构建产物目录由实际技术栈决定。
- 新增初始为空的 `references/` 项目知识目录，用于保存用户主动要求生成或维护的可复用专题知识。
- 明确 references 与 memory、rules、skills 的职责边界，并禁止 AI 在普通任务中自动生成或沉淀参考资料。
- 新增长期 `PLANS.md`，建立人工批准的 Roadmap、即时 exec plan、ready batch、隔离 worktree、任务级验证与集成级验证流程。
- 增加跨平台的 subagent 模型/推理抽象档位、最低充分选档、全新 Agent 逐级升级和 `platform-default` 降级策略。
- 扩展技术债跟踪，规定 AI 只记录证据候选，是否接受、排序或提升由人工决定。

## 0.4.1 - 2026-08-12

- 将 `AGENTS.md`、`rules.md`、`memory.md`、项目 Skill 说明、执行计划文档和变更日志统一改为中文。
- 保留路径、命令、状态枚举、协议返回值和工具名称等技术文本，确保人工可读性与机器解析稳定性兼顾。
- 在项目记忆中记录“说明文档默认使用中文”的持久偏好。

## 0.4.0 - 2026-08-12

- 将可执行脚手架生成器替换为可直接下载的静态 Loop Engineering 模板。
- 增加 Git 启动门禁、规则与记忆外置、可恢复执行计划、仓库级 Skill 隔离、渐进式 linter 与功能测试门禁，以及独立结果验证。
- 删除启动器、Payload 下载、自动初始化、CI、捆绑的运行时 Skill 和特定技术栈工具。
